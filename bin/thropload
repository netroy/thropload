#!/usr/bin/env node

'use strict';


const debug = require('debug')('thropload')
const port = 9090

const { create } = require('..')

create().listen(port, () => {
  debug('server started on - http://0.0.0.0:%s/', port)
})
